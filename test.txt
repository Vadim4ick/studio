 const { scrollYProgress } = useScroll()

  const background = useTransform(
    scrollYProgress,
    [0, 1],
    [
      "linear-gradient(180deg, #000000 0%, #5D2CA8 50%, #000000 100%)",
      "linear-gradient(180deg, #000000 0%, #5D2CA8 50%, #000000 100%)",
    ],
  )

  return (
    <motion.main
      style={{ background }}
      className={"max-desktop:pt-headerMobile"}
    >